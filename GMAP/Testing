import trino

conn = trino.dbapi.connect(
    host='localhost',
    port=8080,
    user='tung',
    catalog='hive',
    schema='default',
)

cur = conn.cursor()
cur.execute("SELECT * FROM my_table LIMIT 10")
rows = cur.fetchall()
print(rows)

